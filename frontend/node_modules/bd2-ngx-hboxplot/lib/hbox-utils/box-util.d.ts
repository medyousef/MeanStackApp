import { BoxDefinition } from './box-dom';
export declare class BoxUtil {
    dataToBoxes(data: number[][]): BoxDefinition[];
    datumToBox(data: number[]): BoxDefinition;
    mockEmptyValues(boxes: BoxDefinition[], missingVal: number): void;
}
